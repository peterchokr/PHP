# 1장: 웹 개발 기초 복습 & PHP 환경 설정

---

## 학습목표

**학습목표**: 웹 개발의 기초를 다시 정리하고 PHP 개발 환경 구축

이 장을 학습하고 나면 다음을 할 수 있습니다:

✅ HTML/CSS/JavaScript의 핵심 개념을 다시 정리할 수 있습니다.
✅ MySQL의 기본 문법을 복습할 수 있습니다.
✅ PHP 개발 환경을 구축하고 설정할 수 있습니다.
✅ 첫 번째 PHP 파일을 실행하고 결과를 확인할 수 있습니다.
✅ PHPMyAdmin을 사용하여 데이터베이스에 접속할 수 있습니다.

---

## 1️⃣ HTML/CSS/JavaScript 핵심 복습

### 1-1 주요 HTML 태그

웹 개발의 기초가 되는 HTML 태그들을 복습합니다.

#### **Form 태그 (사용자 입력)**

```html
<!-- 기본 form 구조 -->
<form method="POST" action="process.php">
    <!-- 입력 필드들 -->
    <input type="text" name="username" placeholder="사용자명">
    <input type="password" name="password" placeholder="비밀번호">
    <input type="email" name="email" placeholder="이메일">
    <input type="submit" value="제출">
</form>
```

**주요 속성:**
- `method`: 데이터 전송 방식 (GET 또는 POST)
  - GET: URL에 데이터 포함 (보안 취약, 길이 제한)
  - POST: 본문에 데이터 포함 (보안 우수, 용량 무제한)
- `action`: 데이터를 받을 서버 파일 경로

#### **Input 태그 (다양한 입력 필드)**

```html
<!-- 입력 필드의 다양한 타입들 -->

<!-- 텍스트 입력 -->
<input type="text" name="name" placeholder="이름을 입력하세요">

<!-- 숫자 입력 -->
<input type="number" name="age" min="0" max="120">

<!-- 이메일 입력 (자동 검증) -->
<input type="email" name="email">

<!-- 비밀번호 입력 (입력값 가림) -->
<input type="password" name="password">

<!-- 체크박스 (중복 선택 가능) -->
<input type="checkbox" name="hobby" value="coding"> 코딩
<input type="checkbox" name="hobby" value="gaming"> 게임

<!-- 라디오 버튼 (1개만 선택) -->
<input type="radio" name="gender" value="male"> 남성
<input type="radio" name="gender" value="female"> 여성

<!-- 버튼 -->
<input type="submit" value="제출">    <!-- form 제출 -->
<input type="reset" value="초기화">     <!-- form 초기화 -->
<input type="button" value="클릭">      <!-- 일반 버튼 -->
```

#### **Table 태그 (표 작성)**

```html
<!-- 기본 테이블 구조 -->
<table border="1">
    <!-- 테이블 제목 행 -->
    <thead>
        <tr>
            <th>제목 1</th>
            <th>제목 2</th>
            <th>제목 3</th>
        </tr>
    </thead>
    
    <!-- 테이블 본문 -->
    <tbody>
        <tr>
            <td>데이터 1-1</td>
            <td>데이터 1-2</td>
            <td>데이터 1-3</td>
        </tr>
        <tr>
            <td>데이터 2-1</td>
            <td>데이터 2-2</td>
            <td>데이터 2-3</td>
        </tr>
    </tbody>
</table>
```

**주요 태그:**
- `<table>`: 테이블 전체
- `<thead>`: 테이블 헤더 (제목 행)
- `<tbody>`: 테이블 본문
- `<tr>`: 테이블 행 (Table Row)
- `<th>`: 테이블 헤더 셀 (Table Header)
- `<td>`: 테이블 데이터 셀 (Table Data)

---

### 1-2 CSS 선택자와 스타일링 기초

CSS를 사용하여 웹 페이지를 꾸미는 기본 방법입니다.

#### **CSS 선택자 종류**

```css
/* ================================================
   1️⃣ 타입 선택자 (태그 이름)
   ================================================ */
p {
    color: blue;
}

/* ================================================
   2️⃣ 클래스 선택자 (. 사용)
   ================================================ */
.highlight {
    background-color: yellow;
    font-weight: bold;
}

/* ================================================
   3️⃣ ID 선택자 (# 사용)
   ================================================ */
#header {
    background-color: navy;
    color: white;
    padding: 20px;
}

/* ================================================
   4️⃣ 속성 선택자
   ================================================ */
input[type="text"] {
    border: 1px solid gray;
    padding: 5px;
}

/* ================================================
   5️⃣ 의사 클래스 (상태에 따른 스타일)
   ================================================ */
a:hover {
    color: red;
    text-decoration: underline;
}

button:active {
    transform: scale(0.95);
}
```

**선택자 우선순위:**
```
ID 선택자 > 클래스 선택자 > 타입 선택자

예시:
#special { color: red; }       /* 우선순위: 100 */
.highlight { color: blue; }    /* 우선순위: 10 */
p { color: green; }            /* 우선순위: 1 */

최종 색상: red (ID 선택자가 가장 높음)
```

#### **기본 스타일링 속성**

```css
/* 텍스트 스타일 */
.text-styles {
    color: #333;                    /* 텍스트 색상 */
    font-size: 16px;               /* 텍스트 크기 */
    font-weight: bold;             /* 텍스트 두께 */
    text-align: center;            /* 정렬 (left, center, right) */
    line-height: 1.5;              /* 줄 높이 */
}

/* 배경 스타일 */
.background-styles {
    background-color: #f0f0f0;     /* 배경 색상 */
    background-image: url('image.jpg');  /* 배경 이미지 */
}

/* 박스 모델 */
.box-model {
    width: 200px;                  /* 너비 */
    height: 100px;                 /* 높이 */
    margin: 10px;                  /* 바깥 여백 */
    padding: 15px;                 /* 안쪽 여백 */
    border: 2px solid gray;        /* 경계선 */
    border-radius: 5px;            /* 모서리 둥글기 */
}

/* 위치와 레이아웃 */
.layout {
    display: flex;                 /* 플렉스 레이아웃 */
    justify-content: center;       /* 가로 정렬 */
    align-items: center;           /* 세로 정렬 */
    position: relative;            /* 위치 (static, relative, absolute, fixed) */
}
```

---

### 1-3 JavaScript DOM 조작

JavaScript를 사용하여 HTML 요소를 동적으로 제어합니다.

#### **요소 선택 (Selecting Elements)**

```javascript
// ============================================
// 1️⃣ ID로 요소 선택
// ============================================
// HTML: <div id="myDiv">내용</div>
const element = document.getElementById('myDiv');

// ============================================
// 2️⃣ 클래스로 요소 선택 (모든 일치 요소)
// ============================================
// HTML: <div class="item">항목 1</div>
//       <div class="item">항목 2</div>
const elements = document.getElementsByClassName('item');
// 결과: HTMLCollection [<div>, <div>]

// ============================================
// 3️⃣ CSS 선택자로 요소 선택 (첫 번째)
// ============================================
const first = document.querySelector('.item');  // 첫 번째 .item

// ============================================
// 4️⃣ CSS 선택자로 모든 요소 선택
// ============================================
const all = document.querySelectorAll('.item');  // 모든 .item
```

#### **이벤트 리스너 (Event Listener)**

```javascript
// ============================================
// 핵심: addEventListener로 이벤트 감지
// ============================================

// HTML: <button id="myButton">클릭하세요</button>

const button = document.getElementById('myButton');

// 클릭 이벤트 등록
button.addEventListener('click', function() {
    alert('버튼이 클릭되었습니다!');
});

// ============================================
// 주요 이벤트 종류
// ============================================

// 1. 마우스 이벤트
element.addEventListener('click', function() {
    // 클릭했을 때
});

element.addEventListener('dblclick', function() {
    // 더블클릭했을 때
});

element.addEventListener('mouseover', function() {
    // 마우스가 요소 위로 올라왔을 때
});

element.addEventListener('mouseout', function() {
    // 마우스가 요소를 떠났을 때
});

// 2. 폼 이벤트
input.addEventListener('change', function() {
    // input 값이 변경되고 포커스를 잃었을 때
});

input.addEventListener('input', function() {
    // input 값이 변경될 때 (실시간)
});

form.addEventListener('submit', function(e) {
    // form이 제출될 때
    e.preventDefault();  // 기본 동작 방지 (페이지 새로고침 방지)
});

// 3. 키보드 이벤트
input.addEventListener('keydown', function(e) {
    // 키를 눌렀을 때
    console.log(e.key);  // 누른 키 확인
});

input.addEventListener('keyup', function() {
    // 키를 뗐을 때
});
```

#### **요소의 내용 변경**

```javascript
const element = document.getElementById('myDiv');

// ============================================
// 1️⃣ 텍스트 변경 (HTML 태그 무시)
// ============================================
element.textContent = '새로운 텍스트';
// 결과: <div>새로운 텍스트</div>

// ============================================
// 2️⃣ HTML 변경 (HTML 태그 포함)
// ============================================
element.innerHTML = '<strong>굵은 텍스트</strong>';
// 결과: <div><strong>굵은 텍스트</strong></div>

// ============================================
// 3️⃣ 속성 변경
// ============================================
element.setAttribute('class', 'highlight');
const value = element.getAttribute('id');

// ============================================
// 4️⃣ 스타일 변경
// ============================================
element.style.color = 'red';
element.style.backgroundColor = 'yellow';
element.style.fontSize = '20px';

// ============================================
// 5️⃣ 클래스 추가/제거
// ============================================
element.classList.add('highlight');      // 클래스 추가
element.classList.remove('highlight');   // 클래스 제거
element.classList.toggle('highlight');   // 토글 (있으면 제거, 없으면 추가)
```

#### **실습 예제: 간단한 웹 페이지 만들기**

이제 배운 내용을 실제로 활용해봅시다. 다음 파일을 만들어서 브라우저에서 실행해보세요.

**파일명: `form_practice.html`**

```html
<!DOCTYPE html>
<html>
<head>
    <title>HTML/CSS/JavaScript 복습</title>
    <style>
        body {
            font-family: '맑은 고딕', sans-serif;
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
        }
        
        .container {
            background: white;
            padding: 30px;
            border: 1px solid #ddd;
        }
        
        h1 {
            color: navy;
            text-align: center;
        }
        
        .form-group {
            margin-bottom: 15px;
        }
        
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }
        
        input[type="text"],
        input[type="email"],
        select {
            width: 100%;
            padding: 8px;
            border: 1px solid #ddd;
            box-sizing: border-box;
        }
        
        button {
            width: 100%;
            padding: 10px;
            background-color: navy;
            color: white;
            border: none;
            cursor: pointer;
        }
        
        #result {
            margin-top: 20px;
            padding: 15px;
            background-color: #e3f2fd;
            border-left: 4px solid navy;
            display: none;
        }
        
        #result.show {
            display: block;
        }
        
        .success {
            color: green;
            font-weight: bold;
        }
    </style>
</head>
<body>

<div class="container">
    <h1>📝 사용자 정보 입력</h1>
    
    <form id="userForm">
        <!-- 이름 입력 -->
        <div class="form-group">
            <label for="name">이름 *</label>
            <input type="text" id="name" name="name" placeholder="이름을 입력하세요" required>
        </div>
        
        <!-- 이메일 입력 -->
        <div class="form-group">
            <label for="email">이메일 *</label>
            <input type="email" id="email" name="email" placeholder="이메일을 입력하세요" required>
        </div>
        
        <!-- 나이 입력 -->
        <div class="form-group">
            <label for="age">나이</label>
            <input type="text" id="age" name="age" placeholder="나이를 입력하세요">
        </div>
        
        <!-- 직업 선택 -->
        <div class="form-group">
            <label for="job">직업</label>
            <select id="job" name="job">
                <option value="">선택하세요</option>
                <option value="student">학생</option>
                <option value="developer">개발자</option>
                <option value="designer">디자이너</option>
                <option value="other">기타</option>
            </select>
        </div>
        
        <!-- 제출 버튼 -->
        <button type="submit">제출하기</button>
    </form>
    
    <!-- 결과 표시 영역 -->
    <div id="result">
        <p class="success">✅ 제출 완료!</p>
        <p id="resultText"></p>
    </div>
</div>

<script>
/**
 * 폼 제출 이벤트 처리
 * 
 * 이 코드는 다음을 수행합니다:
 * 1. form 요소 선택
 * 2. submit 이벤트 리스너 등록
 * 3. 입력한 정보 수집 및 표시
 * 4. 결과 영역 표시
 */

// ============================================
// 1️⃣ 폼 요소 선택
// ============================================
const form = document.getElementById('userForm');
const resultDiv = document.getElementById('result');
const resultText = document.getElementById('resultText');

// ============================================
// 2️⃣ 폼 제출 이벤트 리스너
// ============================================
form.addEventListener('submit', function(e) {
    // 페이지 새로고침 방지
    e.preventDefault();
    
    // ============================================
    // 3️⃣ 입력한 정보 수집
    // ============================================
    const name = document.getElementById('name').value;
    const email = document.getElementById('email').value;
    const age = document.getElementById('age').value;
    const job = document.getElementById('job').value;
    
    // 입력한 정보로 문자열 구성
    let message = `이름: ${name}, 이메일: ${email}`;
    
    // 나이와 직업은 선택사항이므로 입력했을 때만 추가
    if (age) {
        message += `, 나이: ${age}`;
    }
    if (job) {
        message += `, 직업: ${job}`;
    }
    
    // ============================================
    // 4️⃣ 결과 표시
    // ============================================
    resultText.textContent = message;
    
    // 클래스 추가하여 결과 영역 표시
    resultDiv.classList.add('show');
    
    // ============================================
    // 5️⃣ 폼 초기화 (선택사항)
    // ============================================
    form.reset();
});

// ============================================
// 추가 기능: 폼 초기화 시 결과 숨기기
// ============================================
document.querySelector('input[type="reset"]')?.addEventListener('click', function() {
    resultDiv.classList.remove('show');
});
</script>

</body>
</html>
```

**이 파일을 실행하면:**
1. 폼에 정보를 입력
2. "제출하기" 버튼 클릭
3. JavaScript가 입력한 정보를 수집하여 표시
4. 결과 영역에 "✅ 제출 완료!" 메시지와 정보 표시

**배운 개념들이 실제로 어떻게 작동하는지 확인할 수 있습니다!**

---

## 2️⃣ MySQL 기초 복습

### 2-1 기본 SQL 문법

데이터베이스를 조작하는 기본 SQL 명령어들입니다.

#### **테이블 생성 (CREATE TABLE)**

```sql
-- ================================================
-- 기본 구조
-- ================================================

CREATE TABLE table_name (
    column1 datatype constraint,
    column2 datatype constraint,
    ...
);

-- ================================================
-- 실제 예시
-- ================================================

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,      -- 자동 증가하는 주요 키
    name VARCHAR(100) NOT NULL,             -- 최대 100자의 문자열, 필수
    email VARCHAR(100) UNIQUE,              -- 고유한 이메일
    age INT,                                -- 정수형
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP  -- 생성 시간
);
```

**주요 제약 조건:**
- `PRIMARY KEY`: 유일한 식별자 (중복 불가, NULL 불가)
- `NOT NULL`: 값이 반드시 있어야 함
- `UNIQUE`: 모든 값이 유일해야 함
- `AUTO_INCREMENT`: 자동으로 값 증가
- `DEFAULT`: 기본값 설정

#### **데이터 삽입 (INSERT)**

```sql
-- ================================================
-- 1️⃣ 모든 컬럼에 값 삽입
-- ================================================

INSERT INTO users VALUES 
(1, '홍길동', 'hong@email.com', 25, NOW());

-- ================================================
-- 2️⃣ 특정 컬럼만 값 삽입
-- ================================================

INSERT INTO users (name, email, age) 
VALUES ('김영희', 'kim@email.com', 30);

-- 결과: id와 created_at은 자동 생성됨

-- ================================================
-- 3️⃣ 여러 행 한 번에 삽입
-- ================================================

INSERT INTO users (name, email, age) VALUES
('이순신', 'lee@email.com', 35),
('강감찬', 'kang@email.com', 40),
('세종대왕', 'sejong@email.com', 45);
```

#### **데이터 조회 (SELECT)**

```sql
-- ================================================
-- 1️⃣ 모든 데이터 조회
-- ================================================

SELECT * FROM users;

-- ================================================
-- 2️⃣ 특정 컬럼만 조회
-- ================================================

SELECT name, email FROM users;

-- ================================================
-- 3️⃣ WHERE 조건으로 필터링
-- ================================================

-- 나이가 30 이상인 사용자만
SELECT * FROM users WHERE age >= 30;

-- 이름이 '홍길동'인 사용자
SELECT * FROM users WHERE name = '홍길동';

-- 이메일에 '@' 포함 (LIKE 사용)
SELECT * FROM users WHERE email LIKE '%@%.com';

-- ================================================
-- 4️⃣ ORDER BY 정렬
-- ================================================

-- 나이순으로 오름차순 정렬
SELECT * FROM users ORDER BY age ASC;

-- 나이순으로 내림차순 정렬
SELECT * FROM users ORDER BY age DESC;

-- ================================================
-- 5️⃣ LIMIT 개수 제한
-- ================================================

-- 처음 5개만 조회
SELECT * FROM users LIMIT 5;

-- 10번째부터 5개 조회
SELECT * FROM users LIMIT 10, 5;

-- ================================================
-- 6️⃣ COUNT로 개수 세기
-- ================================================

-- 전체 사용자 수
SELECT COUNT(*) FROM users;

-- 이름이 있는 사용자 수
SELECT COUNT(name) FROM users;
```

#### **데이터 수정 (UPDATE)**

```sql
-- ================================================
-- 핵심: WHERE 조건 없으면 모든 행이 수정됨!
-- ================================================

-- ✅ 올바른 예
UPDATE users SET age = 26 WHERE name = '홍길동';
-- 홍길동의 나이만 26으로 변경

-- ❌ 위험한 예
UPDATE users SET age = 26;
-- 모든 사용자의 나이가 26으로 변경됨!

-- ================================================
-- 여러 컬럼 한 번에 수정
-- ================================================

UPDATE users SET 
    name = '김철수',
    age = 28,
    email = 'kim.chulsu@email.com'
WHERE id = 2;
```

#### **데이터 삭제 (DELETE)**

```sql
-- ================================================
-- 핵심: WHERE 조건 없으면 모든 행이 삭제됨!
-- ================================================

-- ✅ 올바른 예
DELETE FROM users WHERE id = 1;
-- id가 1인 사용자만 삭제

-- ❌ 위험한 예
DELETE FROM users;
-- 모든 사용자가 삭제됨! (복구 불가!)
```

### 2-2 기본 JOIN 개념

여러 테이블의 데이터를 함께 조회합니다.

#### **테이블 예시**

```sql
-- 사용자 테이블
CREATE TABLE users (
    id INT PRIMARY KEY,
    name VARCHAR(50)
);

-- 게시글 테이블
CREATE TABLE posts (
    id INT PRIMARY KEY,
    user_id INT,
    title VARCHAR(100),
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

#### **INNER JOIN (내부 조인)**

```sql
-- ================================================
-- 두 테이블에 모두 있는 데이터만 조회
-- ================================================

SELECT users.name, posts.title
FROM users
INNER JOIN posts ON users.id = posts.user_id;

-- 결과:
-- 홍길동    첫 번째 게시글
-- 김영희    두 번째 게시글
-- (게시글이 없는 사용자는 표시 안 됨)
```

#### **LEFT JOIN (좌측 조인)**

```sql
-- ================================================
-- 좌측 테이블(users)의 모든 데이터 + 우측 테이블의 일치 데이터
-- ================================================

SELECT users.name, posts.title
FROM users
LEFT JOIN posts ON users.id = posts.user_id;

-- 결과:
-- 홍길동    첫 번째 게시글
-- 김영희    두 번째 게시글
-- 이순신    NULL (게시글 없음)
-- (모든 사용자가 표시됨)
```

### 2-3 데이터 타입

MySQL에서 사용하는 주요 데이터 타입들입니다.

#### **숫자 타입**

```sql
-- 정수형
INT              -- -2,147,483,648 ~ 2,147,483,647
BIGINT           -- -9,223,372,036,854,775,808 ~ ...
SMALLINT         -- -32,768 ~ 32,767
TINYINT          -- -128 ~ 127

-- 실수형
FLOAT            -- 대략적인 실수 (메모리 효율적)
DECIMAL(10, 2)   -- 정확한 실수 (정수 10자리, 소수 2자리)
```

#### **문자열 타입**

```sql
VARCHAR(100)     -- 가변 길이 문자열 (최대 100자, 실제 길이만 저장)
CHAR(10)         -- 고정 길이 문자열 (항상 10자, 낭비 가능)
TEXT             -- 긴 텍스트 (65,535자까지)
LONGTEXT         -- 매우 긴 텍스트
```

#### **날짜/시간 타입**

```sql
DATE             -- 날짜 (2024-01-15)
TIME             -- 시간 (14:30:45)
DATETIME         -- 날짜와 시간 (2024-01-15 14:30:45)
TIMESTAMP        -- 타임스탬프 (자동 업데이트 가능)
```

### 2-4 실습 예제: PHPMyAdmin에서 직접 테이블 생성

이제 배운 내용을 실제로 PHPMyAdmin에서 구현해봅시다.

**단계 1: PHPMyAdmin 접속**
1. 브라우저에서 `http://localhost/phpmyadmin` 접속
2. 사용자명: `root`, 비밀번호: 입력 (기본값: 공백)
3. 로그인

**단계 2: 데이터베이스 생성**
1. 상단 메뉴에서 "새로 만들기" 클릭
2. 데이터베이스명: `test_db` 입력
3. 정렬 순서: `utf8mb4_general_ci` 선택
4. "만들기" 버튼 클릭

**단계 3: 테이블 생성 및 데이터 삽입**

좌측에서 `test_db` 클릭 → SQL 탭 → 다음 코드 실행:

```sql
-- ================================================
-- 테이블 생성
-- ================================================

CREATE TABLE students (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    email VARCHAR(100),
    age INT,
    grade VARCHAR(10),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ================================================
-- 데이터 삽입
-- ================================================

INSERT INTO students (name, email, age, grade) VALUES
('홍길동', 'hong@example.com', 20, 'A'),
('김영희', 'kim@example.com', 21, 'B'),
('이순신', 'lee@example.com', 22, 'A'),
('강감찬', 'kang@example.com', 20, 'C'),
('세종대왕', 'sejong@example.com', 23, 'A');
```

**단계 4: 데이터 조회 실습**

같은 SQL 탭에서 다음을 차례대로 실행해보세요:

```sql
-- 모든 학생 조회
SELECT * FROM students;

-- 나이가 21세 이상인 학생만
SELECT name, email, age FROM students WHERE age >= 21;

-- 학점이 'A'인 학생만
SELECT name, email, grade FROM students WHERE grade = 'A';

-- 나이순으로 정렬 (오래된 순)
SELECT * FROM students ORDER BY age ASC;

-- 전체 학생 수
SELECT COUNT(*) FROM students;

-- 평균 나이
SELECT AVG(age) FROM students;
```

**단계 5: 데이터 수정 및 삭제 실습**

```sql
-- 학생 정보 수정 (홍길동의 학점을 B로)
UPDATE students SET grade = 'B' WHERE name = '홍길동';

-- 특정 학생 삭제
DELETE FROM students WHERE name = '강감찬';

-- 수정/삭제 후 다시 조회하여 변경사항 확인
SELECT * FROM students;
```

---

## 3️⃣ PHP 환경 설정

### 3-1 개발 환경 확인

#### **필요한 소프트웨어**

```
웹 서버: Apache (또는 Nginx)
프로그래밍 언어: PHP 7.4 이상
데이터베이스: MySQL 5.7 이상 (또는 MariaDB)
데이터베이스 관리 도구: PHPMyAdmin
```

#### **설치 확인 (Windows에서)**

```
1. Apache 설치 확인
   → Apache 실행
   → 브라우저에서 http://localhost 접속
   → "It works!" 메시지 표시 여부 확인

2. PHP 설치 확인
   → C:\php 폴더 존재 여부
   → php.ini 파일 위치 확인

3. MySQL 설치 확인
   → MySQL 서비스 실행 여부
   → 포트 3306 열려 있는지 확인

4. PHPMyAdmin 설치 확인
   → 브라우저에서 http://localhost/phpmyadmin 접속
```

### 3-2 php.ini 기본 설정

PHP의 설정 파일을 확인하고 필요시 수정합니다.

#### **주요 설정항목**

```ini
; ================================================
; 1️⃣ 표준 시간대 설정
; ================================================
date.timezone = "Asia/Seoul"

; ================================================
; 2️⃣ 파일 업로드 설정
; ================================================
upload_max_filesize = 20M        ; 최대 업로드 크기
post_max_size = 20M              ; POST 최대 크기

; ================================================
; 3️⃣ 에러 설정
; ================================================
display_errors = On              ; 에러 화면에 표시
error_reporting = E_ALL          ; 모든 에러 레벨 표시

; ================================================
; 4️⃣ MySQL/MySQLi 확장
; ================================================
; 다음 항목이 주석 해제되어 있어야 함:
; extension=mysqli
; extension=pdo_mysql
```

**php.ini 위치 찾기:**
```php
<?php
// 현재 사용 중인 php.ini의 위치 확인
phpinfo();
?>

// 결과에서 "Loaded Configuration File" 항목 확인
```

---

## 4️⃣ 첫 번째 PHP 파일 실행

### 4-1 "Hello, PHP World!" 프로그램

#### **파일 생성 및 실행**

파일명: `hello.php`

```php
<?php
/**
 * hello.php - 첫 번째 PHP 파일
 * 
 * 역할:
 * 1. PHP 코드 작성 및 실행
 * 2. echo를 사용하여 출력
 * 3. 브라우저에서 결과 확인
 */

// ============================================
// 핵심: echo로 출력하기
// ============================================

echo "Hello, PHP World!";

?>
```

**주목할 점:**
- `<?php ... ?>`: PHP 코드 영역 표시
- `echo`: 출력 명령어
- 파일 저장 위치: Apache 루트 디렉토리 (보통 `C:\xampp\htdocs`)

**실행 방법:**
1. 파일을 Apache 웹 서버가 접근할 수 있는 폴더에 저장
2. 브라우저 주소창에 `http://localhost/hello.php` 입력
3. "Hello, PHP World!" 메시지 표시 확인 ✅

### 4-2 PHP 기본 태그와 출력

```php
<?php
/**
 * basic.php - PHP 기본 문법
 * 
 * 핵심 개념들:
 * 1. PHP 태그
 * 2. 주석
 * 3. echo와 print의 차이
 * 4. 변수 출력
 */

// ============================================
// 1️⃣ echo - 여러 인자 가능
// ============================================

echo "첫 번째 문장";
echo "두 번째 문장";

// 여러 값을 한 번에 출력 (쉼표로 구분)
echo "이름: ", "홍길동", ", 나이: ", 25;

// 결과: 이름: 홍길동, 나이: 25

// ============================================
// 2️⃣ print - 한 번에 하나만
// ============================================

print "이것도 출력됩니다";

// print는 한 번에 하나의 인자만 가능
// print "첫 번째", "두 번째";  // ❌ 오류!

// ============================================
// 3️⃣ 주석 (코드에 설명 추가)
// ============================================

// 한 줄 주석
/* 여러 줄
   주석
   작성 가능 */

# 파이썬 스타일 주석도 가능

// ============================================
// 4️⃣ 변수 선언 및 출력
// ============================================

$name = "홍길동";
$age = 25;
$email = "hong@example.com";

echo "이름: " . $name . "<br>";
echo "나이: " . $age . "<br>";
echo "이메일: " . $email . "<br>";

// 결과:
// 이름: 홍길동
// 나이: 25
// 이메일: hong@example.com

// ============================================
// 5️⃣ 배열 출력
// ============================================

$fruits = array("사과", "바나나", "오렌지");

echo $fruits[0] . "<br>";  // 사과
echo $fruits[1] . "<br>";  // 바나나
echo $fruits[2] . "<br>";  // 오렌지

// ============================================
// 6️⃣ var_dump() - 변수 상세 정보
// ============================================

$value = 123;
var_dump($value);
// 결과: int(123)

$text = "Hello";
var_dump($text);
// 결과: string(5) "Hello"

?>
```

### 4-3 실습 예제: HTML과 PHP 혼합

이제 PHP가 HTML을 생성하는 실제 패턴을 배워봅시다.

**파일명: `grade_list.php`**

```php
<?php
/**
 * grade_list.php - PHP로 동적 HTML 생성
 * 
 * 역할:
 * 1. PHP 배열에 학생 데이터 저장
 * 2. 반복문으로 HTML 테이블 행 생성
 * 3. 동적 콘텐츠 표시
 */

// ============================================
// 1️⃣ 데이터 준비
// ============================================

$students = array(
    array('name' => '홍길동', 'korean' => 85, 'math' => 90, 'english' => 88),
    array('name' => '김영희', 'korean' => 92, 'math' => 88, 'english' => 91),
    array('name' => '이순신', 'korean' => 78, 'math' => 95, 'english' => 85),
    array('name' => '강감찬', 'korean' => 88, 'math' => 87, 'english' => 89)
);

?>

<!DOCTYPE html>
<html>
<head>
    <title>📊 학생 성적 관리</title>
    <style>
        body {
            font-family: '맑은 고딕', sans-serif;
            max-width: 800px;
            margin: 30px auto;
            padding: 20px;
        }
        
        .container {
            background: white;
            padding: 30px;
            border: 1px solid #ddd;
        }
        
        h1 {
            color: navy;
            text-align: center;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        
        thead {
            background-color: navy;
            color: white;
        }
        
        th, td {
            padding: 12px;
            text-align: center;
            border: 1px solid #ddd;
        }
        
        .average {
            font-weight: bold;
            color: navy;
        }
    </style>
</head>
<body>

<div class="container">
    <h1>📊 학생 성적 관리</h1>
    
    <table>
        <thead>
            <tr>
                <th>이름</th>
                <th>국어</th>
                <th>수학</th>
                <th>영어</th>
                <th>평균</th>
            </tr>
        </thead>
        <tbody>
            <?php
            /**
             * 핵심: PHP 반복문으로 HTML 생성
             * 
             * 각 학생 데이터를 순회하면서
             * HTML 테이블 행을 동적으로 생성합니다.
             */
            
            foreach ($students as $student) {
                // 평균 점수 계산
                $average = ($student['korean'] + $student['math'] + $student['english']) / 3;
                $average = round($average, 1);  // 소수점 1자리까지
                
                // 평균에 따라 색상 결정
                $color = ($average >= 90) ? 'green' : (($average >= 80) ? 'navy' : 'red');
                
                // HTML 테이블 행 생성
                echo "<tr>";
                echo "<td>" . $student['name'] . "</td>";
                echo "<td>" . $student['korean'] . "</td>";
                echo "<td>" . $student['math'] . "</td>";
                echo "<td>" . $student['english'] . "</td>";
                echo "<td class='average' style='color: $color;'>" . $average . "</td>";
                echo "</tr>";
            }
            ?>
        </tbody>
    </table>
</div>

</body>
</html>
```

**이 파일을 `http://localhost/grade_list.php`로 실행하면:**
- 학생 성적을 테이블 형식으로 표시
- 평균 점수를 자동 계산
- PHP 반복문이 HTML을 동적으로 생성하는 것을 확인할 수 있습니다 ✅

---

## 5️⃣ PHPMyAdmin을 통한 데이터베이스 관리

### 5-1 PHPMyAdmin 접속

1. **브라우저에서 접속**
   - 주소: `http://localhost/phpmyadmin`
   - 사용자명: `root`
   - 비밀번호: (설정한 비밀번호, 기본값: 공액)

2. **로그인 후 화면**
   - 좌측: 데이터베이스 목록
   - 중앙: 데이터베이스/테이블 관리
   - 상단: SQL 실행 탭

### 5-2 데이터베이스 생성 및 관리

#### **데이터베이스 생성**

```sql
-- ================================================
-- 방법 1: SQL로 생성
-- ================================================

CREATE DATABASE my_database CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;

-- ================================================
-- 방법 2: PHPMyAdmin UI에서 생성
-- ================================================

1. 상단 메뉴에서 "새로 만들기" 클릭
2. 데이터베이스명 입력: "my_database"
3. 정렬 순서: "utf8mb4_general_ci" 선택
4. "만들기" 버튼 클릭
```

---

## ✅ 퀴즈/과제

#### **과제 1: 로컬 환경 구축 완료**

다음을 완료하고 스크린샷을 제출하세요:

1. **Apache 실행 확인**
   - `http://localhost`에서 "It works!" 메시지 표시
   - 스크린샷 첨부

2. **첫 번째 PHP 파일 실행**
   - `hello.php` 생성 및 실행
   - "Hello, PHP World!" 메시지 표시 스크린샷

3. **PHPMyAdmin 접속**
   - `http://localhost/phpmyadmin` 접속
   - 로그인 화면 스크린샷

#### **과제 2: 실습 예제 완성**

위에서 배운 실습 예제들을 직접 구현하세요:

1. **HTML/CSS/JavaScript 예제**
   - `form_practice.html` 생성
   - 폼 제출 시 정보 표시 확인
   - 스크린샷 제출

2. **MySQL 실습**
   - PHPMyAdmin에서 `test_db` 데이터베이스 생성
   - `students` 테이블 생성
   - 5명 이상의 학생 정보 삽입
   - 다양한 SELECT 쿼리 실행 결과 스크린샷

3. **PHP 예제**
   - `grade_list.php` 생성
   - 학생 성적 테이블 표시 스크린샷
   - 선택사항: HTML과 PHP를 더 복잡하게 구성

---

수고했습니다.

조정현 교수(peterchokr@gmail.com)
영남이공대학교

